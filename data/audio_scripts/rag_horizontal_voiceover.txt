RAG 的价值，不是给模型多塞一点资料，
而是让回答建立在可取回的上下文之上。

没有 RAG，模型更容易只靠参数记忆组织答案。
有了 RAG，答案可以先查，再答，再引用。

它适合知识库问答、文档检索、内部资料助手，
尤其适合需要更稳和更新鲜信息的场景。

真正成熟的做法，不是只加一个检索步骤，
而是把召回、筛选、压缩和回答串成完整链路。

所以 RAG 讲的不是模型更聪明，
而是让答案更可追溯、更贴近真实资料。
